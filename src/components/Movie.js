import React from 'react';
import {MovieProvider} from './MovieContext'

const Movie=(props)=>(
  <section>
      <h1>{props.me}</h1>
      <h1>{props.ke}</h1>
  
      </section>
  )
  
  export default Movie;